tasks:
  test-env-persistence:
    phony: true
    commands: |
      export MY_VAR="hello_world"
      echo "Variable: $MY_VAR"
      echo "Again: $MY_VAR"

  test-fail-fast:
    phony: true
    commands: |
      echo "Before error"
      exit 1
      echo "This should never run"

  test-shell-config:
    shell: "sh"
    phony: true
    commands: |
      echo "Shell: $0"

env:
  barfile: bar
  bat: null
tasks:
  foo:
    dependencies:
      - bar
    commands: |
      echo "make foo out of ${barfile}" > foo
      cat bar >> foo
  phony:
    phony: true
    commands: echo "${bat}" > bat
